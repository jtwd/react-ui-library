import React from 'react'

import PasswordInput from '../PasswordInput'

/** Password input standard (with toggle) */
export default function PasswordInputStandard() {
  return (
    <PasswordInput
      htmlId="password1"
      label="Password"
      name="password1"
      onChange={() => {}}
    />
  )
}
