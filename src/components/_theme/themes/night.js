import colors from "../colors";
import { THEME_CONTRAST_DARK } from './index'

const night = {
  contrast: THEME_CONTRAST_DARK,
  colors: {
    primary: colors.yellow,
    secondary: colors.blue,
    tertiary: colors.grey,
    bg: colors.black,
    fore: colors.white,
    alert: colors.red,
    confirm: colors.green
  }
}

export default night